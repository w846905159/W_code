基于当前改善后的完整版本（含历史有界化方案），重新整理如下。

---

## 项目经历

**项目名称**：多智能体客服系统（基于 LangGraph 的流式客服后端）

**技术栈**：Python、LangGraph / LangChain、LangGraph SDK、Flask、ChromaDB、SSE 流式、REST API、asyncio、向量模型（bge-large-zh）

**项目描述**
一个多智能体客服系统的后端实现：用户经 Web 界面提问，系统将请求跨进程交给 LangGraph 服务，按其编排的工作流完成意图路由、领域智能体（产品/技术/账单/投诉/综合客服）处理与回复生成；支持多轮会话、流式打字机回复、反问澄清、越狱/无关请求拦截、会话历史管理与多会话并发。Flask Web 进程与 LangGraph 服务进程解耦，通过 HTTP/SSE 通信，具备完善的异常降级能力。

**个人职责**

1. **工作流编排与意图路由**
   - 基于 LangGraph StateGraph 搭建"入口 → 主管决策 → 领域智能体 → 最终响应"工作流，自定义状态结构（会话ID、意图类型、对话历史、持久化对话、路由决策字段等）。
   - 实现主管节点：用 LLM 结构化决策完成查询路由与"是否需要反问"判定，内置规则关键词回退，LLM 不可用或输出非法 JSON 时自动降级，保证路由稳定可用。
   - 通过条件路由实现"一句话 → 一个领域专家"的派发，并对越狱/无关请求做拦截。

2. **反问澄清闭环（interrupt 机制）**
   - 实现基于 LangGraph `interrupt` / `Command(resume)` 的澄清交互：主管判定信息不足时挂起线程、返回澄清问题，用户补充后在同一持久化线程上恢复执行；用"已澄清"标记防止死循环反问，正确处理挂起与续跑的线程状态。

3. **跨进程通信与流式接口**
   - 封装 Flask 层对 LangGraph API 的调用：线程/助手创建与复用、请求构建、状态拉取与解析。
   - 实现基于 SSE 的流式聊天接口：消费 token 事件转发为前端打字机效果，同时解析 `custom`/`updates` 事件识别澄清与正常回复，并处理流中断、超时时的状态回退读取。

4. **会话管理与历史持久化**
   - 实现多会话管理：会话列表查询、详情/历史解析、单会话清理与重建，组织成与 Web 路由解耦、可复用的业务逻辑层，便于单测与复用。
   - 基于 LangGraph checkpointer + thread_id 实现对话轮次持久化，前端刷新后可还原完整会话。

5. **对话历史有界化（长期记忆）**
   - 针对"历史无限增长导致 state 越滚越大、每次恢复传输过重、早期信息脱离上下文"的问题，实现"滑动窗口 + LLM 摘要"方案：`persisted_dialogue` 恒定保留最近 N 轮，窗口外旧对话由 LLM 压缩为有界摘要存入 `memory`，并让摘要真正进入主管决策与领域智能体的上下文。
   - 效果：state 体积封顶、每次 get_state/resume 传输稳定，早期信息以"大意"留存，同时不污染前端历史展示、成本可控（仅在超窗时触发一次摘要调用）。

6. **异常处理与降级**
   - 统一封装错误返回码、网络异常重试、LLM/主管失败时的规则回退、向量模型缺失时的 TF-IDF 兜底，保证系统在外部依赖异常时仍能返回可用结果。

**项目成果**
- 后端全链路（请求接收 → 跨进程工作流 → 流式返回 → 会话持久化）一次打通，支持多并发会话与稳定流式体验。
- 关键交互（澄清挂起/恢复）与历史长会话均经完整实现与容错验证；通过历史有界化优化了长会话下的状态体积与传输开销。



## 一、压缩版（投递用，3~4 条）

**项目名称**：多智能体客服系统（基于 LangGraph 的流式客服后端）

**技术栈**：Python、LangGraph/LangChain、LangGraph SDK、Flask、ChromaDB、SSE、REST API、asyncio

**职责（精简 4 条）**
1. 用 LangGraph StateGraph 搭建"入口 → 主管决策 → 领域智能体 → 响应"工作流，实现意图路由、多轮上下文管理与对话轮次持久化。
2. 实现主管决策与反问澄清：LLM 结构化判定路由与环境，信息不足时用 `interrupt`/`resume` 挂起线程、用户补充后在同一持久化线程恢复，并做好防循环处理。
3. 实现 Flask 层与 LangGraph 服务的跨进程对接：线程/助手管理、请求构建、SSE 流式转发（token 打字机 + 澄清事件解析 + 流中断回退读取）、会话列表/历史管理。
4. 完善异常降级：LLM/主管失败时规则回退、网络重试、模型缺失时 TF-IDF 兜底，保证依赖异常时系统仍可用。

---

## 二、技术难点与解决（STAR 面试支撑）

### 难点 1：多进程架构下如何实现"流式打字机 + 澄清挂起"并存？
**背景（S）**：Flask Web 进程和 LangGraph 服务是两个独立进程，通过 HTTP 通信；用户既要"逐字生成"的效果，又要在信息不足时"暂停提问、补充后继续"。

**任务（T）**：设计一个接口能同时承载 token 流式推送和澄清中断两种信号。

**行动（A）**：
- 统一走 SSE 流式接口（`/runs/stream`），设置 `stream_mode=["custom","updates"]`。
- 写了一个逐事件解析器：`custom` 事件转成 `type:token` 推给前端做打字机效果；`updates` 事件里检测到 `__interrupt__` 就转成 `type:clarify`（提取澄清问题）返回。
- 澄清恢复单独走"同一线程 + `Command(resume=答案)`"的续跑接口，不再重新发起新 run，保证从断点继续。

**结果（R）**：一个接口同时搞定流式与澄清；前端收到 `clarify` 停止流、弹出输入框，填完 resume 继续。整条链路不断流、能精确定位到挂起点恢复。

---

### 难点 2：主管决策依赖 LLM，LLM 不可用或返回脏数据时怎么办？
**背景（S）**：路由和澄清都靠主管节点调用大模型输出 JSON；大模型可能超时、返回非法 JSON、或乱序。

**任务（T）**：保证依赖异常时系统不崩、路由仍可用。

**行动（A）**：
- 主管决策封装成"LLM 优先 + 规则回退"双链：LLM 异常或 JSON 解析失败时，自动落到关键词匹配路由。
- 对 LLM 输出做容错：用正则提取 JSON、标签做归一化和子串模糊匹配，非法结果回退规则。
- 澄清判断本身也内置规则条件（查询过短 + 领域有可追问项），不依赖 LLM 也能触发/避免循环。
- 向量模型缺失时整体降级为 TF-IDF，检索仍返回结果而非报错。

**结果（R）**：任一外部依赖挂掉，系统都能给出可用回复；关键路径容错率达到预期，线上稳定。

---

### 难点 3：如何避开会话状态在"澄清恢复"后错乱？
**背景（S）**：澄清要"暂停→等待用户→恢复"，若状态没处理好会出现重复反问、上下文丢失。

**任务（T）**：让恢复后的对话上下文连贯，且不再重复追问。

**行动（A）**：
- 用 LangGraph 的 checkpointer 在 interrupt 时持久化线程现场，恢复时在同一线程续跑。
- 用一个"已澄清"标记：澄清过一次后，主管节点和条件路由都跳过再次反问，杜绝死循环。
- 用户补充的内容合并回原始查询、并写入持久化对话记录，保证后续智能体能读到完整上下文。

**结果（R）**：多轮澄清与续聊上下文连贯，无重复反问；从状态拉取也能还原出完整对话历史。

---

需要时我可以把"难点"再对接到具体文件/行号（比如 `service.py` 的 SSE 解析、`graph.py` 的 interrupt 和防循环标记、`supervisor.py` 的规则回退），方便你面试被追问时能落到实现细节上。
