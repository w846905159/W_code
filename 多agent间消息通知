你抓到了关键：这些机制**不是堆砌，而是对应着不同的消费者**。混乱的根源是把"三种不同的通道"混成了一团——它们服务的对象、方向、底层完全不一样。先把它们拆成三条正交通道，再按四种模式套进去。

## 三条正交的"消息通道"

### 通道A：`AgentEvent` 队列 —— 单个 Agent 的"心跳"
任何 Agent（主/子/队友）的循环都往一个 `BlockingQueue<AgentEvent>` 里塞事件（StreamText / ToolUseEvent / ToolResultEvent / LoopComplete / Error），**塞给"谁启动了它"**。这是所有模式的公共底座。`Agent.run()` 返回这个 queue，启动者自己去 poll。

### 通道B：给"父 Agent 模型"看的系统提醒 —— 收敛到唯一的 `notificationFn`
父 Agent 每轮迭代开头统一调 `notificationFn.get()`，把返回的字符串作为 system reminder 注入对话（`Agent.java:235-238`）。**所有"父模型需要知道的外部消息"最终都汇聚到这里**。在 `MewCodeModel.java:581-591`，这个 callback 里同时 drain 了：
```
notificationFn = () -> {
    notes.addAll(drainLeadMailbox(teamManager));          // 协调模式 Mailbox
    for (var n : subAgentTaskManager.drainNotifications()) // 后台任务通知
        notes.add("<task-notification>...");
    return notes;
};
```
**这一步是关键：协调 Mailbox 和后台任务通知，最终都变成父模型的 system reminder。**

### 通道C：给"UI"看的展示 —— 渲染层
`SubAgentProgress` → `subAgentProgressQueue` → 聊天区 sub_agent 块；`TeammateProgress` → `renderTeammateTree`。**这俩不是"agent 间的消息"，纯粹是 UI 展示**，只是借了消息的壳。

---

## 四种模式的消息流

### 模式1：主从·同步 `runSync`
```
子Agent循环 → AgentEvent队列 → runSync自己poll → 转成 SubAgentProgress(通道C)
                                     └─> 收集 output → 作为 ToolResult 返回给父Agent(tool调用栈内联)
```
- 子代理**阻塞在工具调用里**，结果直接通过 `ToolResult` 同步带回。
- 中间进度用 SubAgentProgress 推给 UI。
- **不涉及通道B**（父模型不用等通知，它被同步阻塞了）、不涉及 Mailbox。

### 模式2：主从·后台 `runAsync`
```
子Agent (虚拟线程) --完成--> SubAgentTaskManager.setCompleted/Failed
                            → TaskNotification 列表
父Agent下一轮迭代 --> notificationFn(通道B) drainNotifications()
            --> <task-notification> 系统提醒 --> 父模型知道"task_3完成，结果..."
```
- 父不阻塞，干完用 TaskNotification 通知父模型。
- **不走 SubAgentProgress**（后台循环只收集 output 文本，不 emit progress）。

### 模式3：主从·后台 `runFork`
```
父对话+replacementState --复制--> forkedConv(上下文继承)
子Agent (虚拟线程) --完成--> SubAgentTaskManager notifications → 通道B → 父模型
```
- **和模式2机制完全相同**（都是 TaskNotification→notificationFn），区别只在**入参**：fork 继承父对话历史 + 字节一致的替换状态。

### 模式4：协调 `team`
```
队友A <─TASK/NOTICE/IDLE/SHUTDOWN─> FileMailBox(磁盘JSON) <─>
队友B                                            │
                                              Lead信箱
父Agent(Lead)每轮 --> drainLeadMailbox(通道B) --> <team-notification> 系统提醒
live UI: TeammateProgress → 队友树(通道C)
Spawn时: drainTeammateEvents → SubAgentProgress"teammate"(通道C)
```
- **成员↔成员**：`FileMailBox`（磁盘 JSON、跨进程，是唯一能跨进程的通道）。
- **Lead 模型**：靠 `drainLeadMailbox` 汇入通道B。

---

## 把它们按"消费者"归类

| 通道 | 底层载体 | 生产者→消费者 | 能跨进程？ | 目的 |
|---|---|---|---|---|
| **AgentEvent 队列** | 内存 BlockingQueue | Agent循环→启动者(工具/TUI) | ❌ | 单 Agent 运行吐事件 |
| **SubAgentProgress** | 进程内 queue(Channel C) | 子/队友→UI | ❌ | 实时进度展示 |
| **SubAgentTaskManager.notifications** | 内存 ArrayList | 后台任务线程→父Agent模型(通道B) | ❌ | 后台任务完成通知 |
| **FileMailBox** | 磁盘 JSON + 锁 | 任意成员→任意成员 | ✅ | 协调通信（任务/通知/停机） |
| **TeammateProgress** | 进程内 | 队友→UI | ❌ | UI 状态树 |

## 一句话把"乱"解开

> **真正"agent 间传播消息"的只有两条**：`FileMailBox`（协调，跨进程，成员互发）+ `SubAgentTaskManager.notifications`（主从后台，任务完成上报）。而这两条**最终都汇进父 Agent 唯一的 `notificationFn`（通道B）变成系统提醒**。`SubAgentProgress` 和 `TeammateProgress` 根本不是 agent 间消息，只是 UI 展示的载体。`AgentEvent` 队列则是所有模式共享的"Agent 自身心跳"底座。

所以不是"很多套消息系统"，而是 **一套跨进程 Mailbox + 一套进程内任务通知，都灌进父模型的同一根通知管，外加两套 UI 展示载体**——这就是全部。






协调模式：：

读完关键代码,给你一个**端到端的完整示例**。先点明协调模式的本质:**它不是开关命令,而是"状态驱动"的**——只要 `TeamManager` 里存在任意一个队伍,Lead 主 Agent 的工具就自动被收紧成协调工具集。

```java
// MewCodeModel:585-588
agent.setToolNameFilter(name ->
        teamMgrCapture.listTeams().isEmpty()      // 无队伍 → 不限制
            ? true
            : Coordinator.isCoordinatorTool(name)); // 有队伍 → 只留协调工具
```

`Coordinator.ALLOWED_TOOLS` = `{Agent, SendMessage, TaskCreate/Get/List/Update, TeamCreate, TeamDelete, ReadFile, Glob, Grep, Bash}`——**没有 WriteFile/EditFile**。也就是说:队伍一落地,Lead 就不能再亲手改代码,只能"指挥 + 只读研究 + 跑命令"。

---

## 场景:让 3 个 Agent 协同重构 `src/agent` 模块

用户对主 Agent(后面称 **Lead**)说:"用团队方式重构 agent 模块,一个探索 API、一个写实现、一个补测试。"

### Step 1 Lead 创建队伍(协调模式在此刻自动"解锁")

Lead 本回合工具还是全量,它调用:

```
TeamCreate({ team_name: "refactor-squad", description: "重构 agent 模块" })
```

```java
// TeamCreateTool.execute → TeamManager.detectBackend()=IN_PROCESS
TeamManager.Team team = teamMgr.createTeam("refactor-squad", mode);
// Team 内含 FileMailBox: .mewcode/teams/refactor-squad/inboxes/
```

**从这个 turn 结束起,`listTeams()` 非空,Lead 下一轮的工具就被 `Coordinator` 过滤收窄。**

### Step 2 Lead 先自己做只读研究 + 拆任务

Lead 用 `ReadFile/Grep/Glob` 探查,然后建立**共享任务清单**(`SharedTaskStore`,带 `blocks/blockedBy` 依赖):

```
TaskCreate({ name: "探索 Agent 公共API", blocks: [] })
TaskCreate({ name: "重构核心循环",       blocks: ["探索 Agent 公共API"] })
TaskCreate({ name: "补重构测试",         blocks: ["重构核心循环"] })
```

### Step 3 Lead 派发队友(关键一步:必须带 team_name)

```
Agent({
  team_name: "refactor-squad",
  name: "explorer",
  prompt: "探索 com.mewcode.agent 的公共 API,输出依赖清单到 lead。",
  description: "API 探索员"
})
```

当 Lead 调用这个 `Agent` 工具时走的是 `AgentTool.runAsTeammate`,内部链路:

```
runAsTeammate
 ├─ 1. ToolFilter.filterForAgent(...) 构建受限注册表
 │      (in-process 队友额外放行 Task*/SendMessage;无 WriteFile?→ 需再确认,但至少专用工具会注入)
 ├─ 2. subRegistry.register(new SendMessageTool(teamMgr, "explorer"))   ← 每个成员一套专属"发信工具",sender 绑定自己
 ├─ 3. buildTeammateAddendum(...)：
 │      "你是 team refactor-squad 的成员,名叫 explorer;lead 可用 SendMessage 联系;队友有 xxx"
 │      → 作为 system-reminder 塞进该成员会话
 └─ 4. SpawnDispatcher.spawnTeammate
        └─ IN_PROCESS → InProcessBackend.startInProcessMember
             ├─ team.addMember(explorer)           // 登记进 Team.members
             └─ Thread.ofVirtual().start(() -> TeammateRunner.runInProcessTeammate(...))
```

**队友一登台就进入自己的 4 步生命周期循环**(TeammateRunner.java:58-92):

```java
while (!interrupted) {
    1. injectPendingMessages(team, name)   // 未读收件箱 → 折叠成 system-reminder
    2. if (nextPrompt != null) member.conv.addUserMessage(nextPrompt)   // 首轮=initialPrompt
    3. member.agent.run(member.conv)       // 跑一整轮到 LoopComplete(事件经 eventOut 转发给 UI)
    4. team.sendMessage(me, "lead", idle通知, IDLE)   // 向 lead 报"空闲"
    5. waitForNextPromptOrShutdown(...)    // 500ms 轮询;TASK 消息唤醒;收到 [shutdown] 退出
}
```

以此类推,Lead 再 `Agent({team_name, name:"implementer", prompt:...})` 和 `Agent({team_name, name:"tester", ...})` 派两个队友。

### Step 4 队友干活与异步通信(全部走文件邮箱)

- 队友中途要信息:`SendMessage({to:"lead", content:"探索到 5 个公共类,详见...", kind:"notice"})` → `FileMailBox` 写入 lead 的 inbox(`kind:"notice"` 不唤醒闲置者,只是备忘)。
- 队友交活:`SendMessage({to:"lead", content:"implementer 完成核心重构,...", kind:"task"})` → **`kind:"task"` 会唤醒闲置状态的 lead**。
- **Lead 怎么看到**:每轮 `agentLoop` 开头 `notificationFn.get()`(MewCodeModel:576-582)→ `TeammateRunner.drainLeadMailbox(teamMgr)` 把各队 lead 收件箱未读消息折叠成:

```
<team-notification team="refactor-squad">
from=explorer: xxx
from=implementer: xxx
</team-notification>
```
上回合结束后 TUI 还有 2s 一次的 `MailboxPollMessage` 轮询触发新一轮唤醒(1428)。

### Step 5 Lead 汇总、再下指令、验证

Lead 看到 `<team-notification>` → 决定下一步:改需求 → 再 `Agent`/`SendMessage` 到对应成员(`kind:task` 唤醒);检查质量 → 用只读工具 + 让 tester 跑测试回报;有冲突 → `SendMessage` 协调。

### Step 6 收尾:`TeamDelete` 一键停机 + 协调模式自动解除

```
TeamDelete({ team_name: "refactor-squad" })
```
```java
// TeamManager.deleteTeam → team.stopAll()
for (member : members) inbox 追加 "[shutdown]"   // TeammateRunner: 收到 → break 循环 → member.active=false
```

**`listTeams()` 变空 → `Coordinator` 过滤自动解除,Lead 恢复全量工具**(WriteFile/EditFile 回来了),可以继续亲手收尾。

---

## 一图流

```
用户: 组个队重构
   │
[Lead] TeamCreate refactor-squad ──◆ 此刻起 Lead 工具 → Coordinator 白名单
   │     TaskCreate×3(共享清单)
   │     Agent(team_name, explorer) ─▶ 虚线程起 TeammateRunner
   │     Agent(team_name, implementer) ─▶ 虚线程
   │     Agent(team_name, tester) ─▶ 虚线程
   │       └─ 各自循环: 收信→干活→发IDLE→休眠
   │  ▲                            │ SendMessage→lead inbox
   └──┴─ notificationFn 每轮折叠成 <team-notification> 注入 Lead
         汇总/协调 → 再 task 唤醒指定队友
   TeamDelete ──◆ Team 数归零 → Lead 恢复全量工具
```

**核心要点**:协调模式 = "存在队伍即自动限制 Lead 为协调者";队友 = 独立的 `TeammateRunner` 生命周期;通信 = 文件邮箱(进程内外一致);唤醒 = `kind:task` 消息 + Lead 侧 `notificationFn/MailboxPoll`;收尾 = `TeamDelete` 触发各成员 `[shutdown]`。四阶段(研究→综合→执行→验证)就是 Lead 在协调工具集内偏好遵循的工作流,写在 `Coordinator` 的 javadoc 里。
